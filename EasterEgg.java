
class EasterEgg 
{
	
	/* Dette er koden til easterEgg metoden som er skrevet i Unicode i SingleLinkedList.java! */
	
	/*
	
	public void easterEgg()
	{
			
		Node cp = head;
		Node pp = null;

		while(cp != null && pp != head)
		{
			Node next = cp;
			for( ; next.next != pp ; next = next.next)
			{
				if(next.element >= next.next.element)
				{
					int temp = next.element;
					next.element = next.next.element;
					next.next.element = temp;
				}
			}
			
			pp = next;
			cp = head;
		}	
			
	} 
	
	*/
	/*
	 	public void easterEgg()
	 	{
	 	
	 	Node cp = head;
	 	Node pp = null;
	 	
	 	while(cp!=null & pp!=head)
	 	{
	 		Node temp = cp;
	 		
	 		while(temp.next != pp)
	 			temp = temp.next;
	 			if(temp.element >= temp.next.element)
	 			{
	 				int temp = temp.element;
	 				temp.element = temp.next.element;
	 				temp.next.element = temp;
	 			}
	 		
	 		pp = temp;
	 		cp = head;
	 	
	 	}
	 	
	 	}
	 
	  */
	
}
